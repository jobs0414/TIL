import pandas as pd 
from xlrd import open_workbook

output_workbook = Workbook() 
output_worksheet = output_workbook.add_sheet('sum_average')

all data=[]
sales_column_index = 3 

header = ['workbook','worksheet','worksheet_total','worksheet_average' ,\ 
                    'workbook_total','workbook_average']
all_data.append(header)

workbook_total = sum(list of_totals)
workbook_average = sum(list_of_totals) / sum(list of numbers)
for list_element in workbook_output: 
    list_element.append(workbook_total)
    list_element.

    number_of_sales =  0 
    worksheet_list = [] 
    worksheet_list.append(os.path.basename(input_file))
    worksheet_list.append(worksheet.name)
    for row_index in range(1, worksheet.nrows):


















for row_index in range(1,worksheet.nrows): 
    try: 
        total_sales += 
float(str(worksheet.cell_value(row_index,sales_column_index)).strip('$').replace(',','')) 



